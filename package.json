{
  "name": "mygym",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach -pvit run build",
    "dev": "yarn workspaces foreach -pvit run dev"
  },
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0"
}
